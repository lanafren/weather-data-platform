version: 2

sources:
  - name: raw
    database: de101-477411
    schema: raw

    tables:
      - name: forecast_raw
      - name: current_raw
    config:
      freshness:
        warn_after: {count: 5, period: hour}
        error_after: {count: 8, period: hour}